"""Pulls full room history via Band REST API (endpoint verified in Phase 0,
recorded in shared/protocol.md §A.4), parses the fenced JSON blocks each
agent emits, and writes incident-report-{id}.md. Phase 6 stub."""
